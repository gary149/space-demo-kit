# Experiments (not for production)

Rejected style explorations kept for reference only. Nothing in this folder is
wired into `render.py`: the renderer always uses `templates/app-window.html`,
and a `"style"` key in a manifest is ignored.

Do not copy these manifests as examples for new demo videos.
